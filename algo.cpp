// counting sort works when elements of array is less than n
	1) count the array element into count array 
	2) sum the adjacent of count to get the index of element in sorted array
	3) parse through original array and place that element into new array with indexing as count says